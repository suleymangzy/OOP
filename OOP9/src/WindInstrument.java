public interface WindInstrument {
    /**
     * Interfaces do not hold state.
     * They do not have object variables because
     * they cannot create objects and their implementation
     * cannot be inherited.
     */
    void breath();
}
